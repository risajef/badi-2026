// Reserved for future scroll-based animations and interactions.
// The poster is composed of individually positioned elements
// (see .el classes in css/styles.css), so each piece can be
// animated independently later (e.g. via IntersectionObserver
// or a scroll-linked timeline).
